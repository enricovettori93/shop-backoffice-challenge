const Footer = () => {
    return (
        <footer className="fixed bottom-0 w-screen h-40 bg-slate-100 flex justify-center items-center z-10">
            questo é un footer :)
        </footer>
    );
};

export default Footer;
